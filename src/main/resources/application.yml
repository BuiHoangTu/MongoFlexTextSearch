server:
  port: 8080

spring:
  application:
    name: MongoFlexFullTextSearch
  data:
    mongodb:
      host: ${MONGODB_HOST}
      username: ${MONGODB_USERNAME}
      password: ${MONGODB_PASSWORD}
      port: ${MONGODB_PORT}
      database: text_search
      authentication-database: ${MONGODB_AUTH_DB}

special:
  org:
    watching:
      databases:
        database_name:
          collection_name:
            text_fields:
              - amo_type
              - amo_source
          name2:
            textFields: other_text
        north:
          name:
            textFields: air_craft